

/**
 * @author NAJIM
 */
object MyProgram {
  def main(args: Array[String]):Unit ={
    println("scala rocks again!");
    val t=("najim",27)
    val (name,age) = t;
    age=readInt;
    println(name,t._1);
  }
} 